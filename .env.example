# Example env for production
NODE_ENV=production
# In EasyPanel, set PORT via environment variable. If not set, server falls back to 8080 for local dev.
# PORT=8080
#HB_BASIC_AUTH=base64credentials
#VITE_API_URL=https://your-api
